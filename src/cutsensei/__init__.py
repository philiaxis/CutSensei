"""CutSensei - automatic editor for lecture videos.

Speech is kept at normal speed, silent board writing is sped up and idle
waiting time is removed. The package is split into a Qt-free core
(``core``, ``analysis``, ``render``) and the desktop UI (``ui``).
"""

__version__ = "0.1.0"
APP_NAME = "CutSensei"
